#include <stdio.h>
#include <stdlib.h>

void print_array(int array [], int size)
{
    for(int i=0; i<size; i++)

            {
                printf("%d ", array[i]);
            }
}

int main()
{
    int a[] = {-9, -4, -8, 2};
    int size = 4;
    print_array(a, size);
    return 0;
}
