#include <stdio.h>

void print_multi(int base, int start, int end)
{

	for (int i=start; i<=end; i++)
    {
		printf("%2d * %2d = %3d\n", base, i, base*i);
	}

}

float main()
{
    print_multi(2,10,20);
}
